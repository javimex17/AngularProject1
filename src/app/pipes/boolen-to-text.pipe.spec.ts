import { BoolenToTextPipe } from './boolen-to-text.pipe';

describe('BoolenToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolenToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
